#include "csp/process.h"
#include "csp/control.h"

#include <stdio.h>

int main(int argc, char **argv)
{
        if (argc != 2) {
                fprintf(stderr, "Usage: count_t <nr of processes>\n");
                exit(1);
        }

        
}
